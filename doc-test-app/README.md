# Encrypted Counter

> This contract demonstrates a simple counter using Homomorphic Encryption.

## Overview
This example demonstrates FHEVM capability using **EncryptedCounter**.
It is generated automatically using the FHEVM Example Hub.

## 🚀 How to Run

### 1. Install Dependencies
```bash
npm install
```

### 2. Run Tests
```bash
npx hardhat test
```

## 📜 Smart Contract Logic
The contract is located at `contracts/EncryptedCounter.sol`.

### Key Functions
- `function add(einput _encryptedAmount, bytes calldata _inputProof) public`
- `function getCounter() public view returns (euint32)`

---
*Generated by FHEVM Example Hub*