# Confidential ERC-20 Token

> A simplified ERC-20 token where balances and transfers are encrypted.

## Overview
This example demonstrates FHEVM capability using **ConfidentialToken**.
It is generated automatically using the FHEVM Example Hub.

## 🚀 How to Run

### 1. Install Dependencies
```bash
npm install
```

### 2. Run Tests
```bash
npx hardhat test
```

## 📜 Smart Contract Logic
The contract is located at `contracts/ConfidentialToken.sol`.

### Key Functions
- `function mint(uint64 amount) public`
- `function transfer(address to, einput encryptedAmount, bytes calldata inputProof) public returns (bool)`
- `function _transfer(address from, address to, einput encryptedAmount, bytes calldata inputProof) internal returns (bool)`
  - *Internal transfer logic*
- `function balanceOf(address wallet) public view returns (euint64)`
  - *Returns the encrypted balance of the caller*

---
*Generated by FHEVM Example Hub*